# 2026-06-10 Memory Log

## Accomplished
1. **À La Carte Menu 2026 Update**:
   - Analyzed `Final Menu A la carte 2026.pdf` pages 2 to 7.
   - Updated the À La Carte database schema seeding script (`scripts/reset_menu_v3.mjs`) to include all 26 final items.
   - Provided full 6-language translations (Vietnamese, English, French, Japanese, Korean, Traditional Chinese / Hong Kong) for names and descriptions of all 26 dishes.
   - Updated prices to match the final values in the PDF (e.g. Tuna Carpaccio: 300k, Crab Salad: 495k, Scallop Tartare: 495k, Wellington: 535k, Miso Cod: 855k, etc.).
   - Re-linked appropriate European standard allergen tags (e.g. GLUTEN, MILK, EGGS, FISH, CRUSTACEANS, MOLLUSCS, SULPHITES) for all dishes.
   - Successfully ran `reset_menu_v3.mjs` to update the Supabase database (preserving Set Menu Prices).

2. **Drinks Page Implementation & Layout Redesign**:
   - Extracted all beverage listings from the last three pages (pages 8, 9, 10) of `Final Menu A la carte 2026.pdf`.
   - Created a dedicated, beautiful drinks list page at `/menu/drinks` (`app/menu/drinks/page.js`) featuring 13 grouped drink categories.
   - Excluded all beverage pricing tags per the user's instructions (prices will be added later).
   - Provided comprehensive 6-language translations for all beverage descriptions and names to match the premium localized user experience.
   - **Dropdown & Bottom Pagination**: Replaced the overflowing horizontal categories filter with a custom luxury dropdown selector (with a chevron down indicator). Added "Next" and "Back" pagination buttons at the bottom of the list to navigate page-by-page through categories seamlessly.

3. **Routing & Deployment**:
   - Checked that navigation links for `/menu/drinks` are properly integrated into `components/Header.js`.
   - Built the Next.js app locally (`npm run build`) to ensure successful compilation.
   - Staged, committed, and pushed changes to the `main` branch on GitHub (`origin/main`), triggering automatic Vercel deployment.

4. **Header Font Size Adjustments**:
   - Increased desktop navigation menu font size from `12px` (`text-[12px]`) to `14px` (`text-[14px]`) in `components/Header.js`.
   - Increased dropdown menu items font size from `11px` (`text-[11px]`) to `13px` (`text-[13px]`).
   - Increased language dropdown options and Booking CTA buttons font size by 2px (to `14px`).
   - Increased mobile menu main headers from `text-sm` to `text-base` and mobile sub-items from `text-xs` to `text-sm` (+2px).

5. **Theme Colors Customization**:
   - Analyzed the styles of the official website `https://www.maisonvie.vn/` and extracted their luxury moss green (xanh rêu) color codes.
   - Set the main page background (`--color-dark-500` & `--background`) to the visibly greener `#111d22`, and cards/dropdowns (`--color-dark-400`) to the darker `#0c1315`.
   - Updated the glassmorphism semi-transparent background to use the moss green tint (`rgba(17, 29, 34, 0.7)`).

6. **Unified Footer & Bo Cong Thuong Badge**:
   - Created a reusable `components/Footer.js` component with brand slogan, opening hours (with manual confirmation peak hours note), contact details, and the mandatory Ministry of Industry and Trade (Bo Cong Thuong) registration logo (linking to the official registry details page).
   - Replaced inline footer elements in all 14 pages of the application with `<Footer lang={lang} />`.

7. **Hotline & Address Updates**:
   - Replaced the hotline globally across all pages and footer translation files with `+84 904150383`.
   - Updated the address globally from "Quận Hai Bà Trưng" (Hai Ba Trung District) to "Phường Hai Bà Trưng" (Hai Ba Trung Ward) in all 6 translation languages to match the user's correct local designation.
   - Ran compilation tests and pushed changes to GitHub.

